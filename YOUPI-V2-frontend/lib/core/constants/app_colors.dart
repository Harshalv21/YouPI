import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color backgroundPrimary = Color(0xFF0A0C10);
  static const Color backgroundSurface = Color(0xFF111318);
  static const Color backgroundCard = Color(0xFF181C24);

  // Accents
  static const Color primary = Color(0xFF00D4A1);
  static const Color secondary = Color(0xFFF5C842);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFF8A9BB0);

  // Status
  static const Color success = Color(0xFF00D4A1);
  static const Color warning = Color(0xFFF5C842);
  static const Color error = Color(0xFFFF4D6A);

  // UI
  static const Color divider = Color(0xFF1E2530);
  static const Color bottomNavActive = Color(0xFF00D4A1);
  static const Color bottomNavInactive = Color(0xFF8A9BB0);

  // Glow
  static const Color primaryGlow = Color(0x3000D4A1);
  static const Color cardBorder = Color(0xFF1E2530);

  // Virtual Card gradient
  static const Color cardGradientStart = Color(0xFF1A1A2E);
  static const Color cardGradientEnd = Color(0xFF16213E);

  // Gold-specific
  static const Color goldText = Color(0xFFF5C842);
  static const Color goldBg = Color(0x20F5C842);
}
